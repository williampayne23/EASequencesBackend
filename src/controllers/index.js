const linkController = require('./links');
const sequenceController = require('./sequences');
const sequenceLinkController = require('./sequencelinks');
const verificationController = require('./verification');

module.exports = {
	linkController,
	sequenceController,
	sequenceLinkController,
	verificationController
};